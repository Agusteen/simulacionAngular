import { SetNoValuePipe } from './set-no-value.pipe';

describe('SetNoValuePipe', () => {
  it('create an instance', () => {
    const pipe = new SetNoValuePipe();
    expect(pipe).toBeTruthy();
  });
});
