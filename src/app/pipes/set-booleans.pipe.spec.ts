import { SetBooleansPipe } from './set-booleans.pipe';

describe('SetBooleansPipe', () => {
  it('create an instance', () => {
    const pipe = new SetBooleansPipe();
    expect(pipe).toBeTruthy();
  });
});
